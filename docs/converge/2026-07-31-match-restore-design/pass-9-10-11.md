# Pass 9 — standard (two-device design flow): CLEAN

Device 1 restored from a file through the UI, claimed Blue, and commanded Valiant only
(Crimson read-only). Device 2 entered the same room code, saw Blue's seat disabled as taken,
claimed Red, and commanded Crimson only. Presence reported Blue offline / Red online.

# Pass 10 — standard (scope and suppression integrity): CLEAN

- Zero file or database I/O introduced in the API or Application projects: the Scope line
  "does not add durable server-side persistence" holds literally.
- No autosave, self-recovery, or fleet-transfer code exists — the suppression set is still absent
  rather than quietly implemented.
- The commitment rebuild hashes with a fresh random salt, so no secret is reconstructed; the
  restore log entry records only the saved timestamp and room code.
- `scripts/verify.ps1` green end to end, 0 vulnerabilities.

# Pass 11 — adversarial: CLEAN

Targeted what the friendly passes had not executed:

- **The `Reveal` row of the design's phase table** had no test — only `OrdersLocked` did. Verified:
  exported `Reveal` restores as `OrderEntry` with commitments cleared, no revealed orders, and the
  explanatory log entry.
- **A destroyed ship** comes back destroyed with hull at max.
- **A restored `FleetSetup` match stays playable**: both seats claimed, both readied, reaching
  order entry with the wreck not blocking the gate.

No gaps. The two behaviours verified here by probe were then pinned with regression tests
(`RestoreMatch_FromRevealExport_FallsBackToOrderEntry`,
`RestoreMatch_KeepsDestroyedShipsDestroyedAndStaysPlayable`) — hardening, not a gap fix.

**clean_streak = 3 (9, 10, 11). CONVERGED.**
